[project]
name = "agent-feature-delivery-pipeline"
version = "1.0.0"
description = "Requirements-driven parallel pipeline for API docs, test suites, and code review"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "toml>=0.10",
]

[project.optional-dependencies]
full = [
    "fastmcp>=0.1",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent_feature_delivery_pipeline"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]
