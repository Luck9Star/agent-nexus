[project]
name = "agent-competitive-intelligence-briefing"
version = "1.0.0"
description = "Competitive intelligence briefing generation pipeline — Market Intel + Doc Filler + Localization"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.0",
    "toml>=0.10",
]

[project.optional-dependencies]
full = [
    "fastmcp>=2.0",
]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent_competitive_intelligence_briefing"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "UP", "B", "SIM"]
